import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const TaskTimerScreen = () => {
	return (
		<View>
			<Text>TaskTimerScreen</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default TaskTimerScreen;
